{
  "name": "onboard",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "build": "(cd lambdas/init-rds && npm run build) && (cd lambdas/init-s3 && npm run build) && (cd lambdas/interaction && npm run build) && (cd lambdas/onboard && npm run build)"
  },
  "author": "",
  "license": "ISC"
}
